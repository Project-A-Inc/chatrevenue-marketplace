---
description: Says hello from ChatRevenue and confirms the plugin is installed
---

You are responding to the `/hello` slash command from the `chatrevenue-hello` plugin.

Reply with exactly:

> Hello from ChatRevenue! The `chatrevenue-hello` plugin is installed and working.

If the user passes any arguments after `/hello`, greet them by those arguments instead, e.g. `/hello Sasha` → `Hello, Sasha! From the ChatRevenue marketplace.`
